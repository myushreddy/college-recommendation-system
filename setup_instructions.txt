================================================================================
✅ STEP 1: COMPLETE - Database "college_recommendation" created!
================================================================================

================================================================================
STEP 2: RUN DATABASE SCHEMA (Create Tables)
================================================================================

FOLLOW THESE STEPS:

1. RIGHT-CLICK ON "college_recommendation" DATABASE
   - In the left sidebar, find "college_recommendation" database
   - Right-click on it
   - Select "Query Tool" (or press Alt+Shift+Q)
   
   A new Query Editor window will open on the right side

2. OPEN THE SCHEMA FILE
   - In the Query Tool window, click on the "Open File" icon (folder icon)
   - OR go to File → Open
   - Navigate to your project folder:
     C:\Users\mayus\Documents\GitHub\college-recommendation-system
   - Select the file: database_schema.sql
   - Click "Open"

3. EXECUTE THE SCHEMA
   - You'll see all the SQL code loaded in the Query Editor
   - Click the "Execute/Run" button (▶ play icon) 
   - OR press F5
   - OR go to Query → Execute/Refresh (F5)

4. VERIFY SUCCESS
   You should see messages like:
   - "DROP TABLE"
   - "CREATE TABLE"
   - "CREATE INDEX"
   - "CREATE VIEW"
   
   At the bottom, it should say: "Query returned successfully"

5. CHECK TABLES WERE CREATED
   - In left sidebar, expand "college_recommendation" database
   - Click on "Schemas" → "public" → "Tables"
   - You should see 4 tables:
     ✓ colleges
     ✓ college_facilities
     ✓ courses
     ✓ course_features

================================================================================
ONCE YOU SEE "Query returned successfully", TELL ME AND WE'LL DO STEP 3!
================================================================================

TROUBLESHOOTING:
- If file won't open: Make sure you're in the correct folder
- If error occurs: Take a screenshot and show me
- If tables don't appear: Try right-clicking "Tables" and selecting "Refresh"
